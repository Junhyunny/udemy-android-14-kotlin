# 저장소 작업 지침

## 프로젝트 목적

- 이 저장소는 Android 14와 Kotlin 강의를 따라가며 작성한 예제와 학습 내용을 관리한다.
- 각 챕터가 독립 Android 프로젝트인 경우 해당 챕터의 Gradle 설정과 코드 스타일을 우선한다.
- 강의의 학습 단계와 예제 의도를 보존하고, 요청받지 않은 대규모 리팩토링이나 앞선 API 적용을 하지 않는다.

## Android/Kotlin 작업

- 대상 챕터의 `settings.gradle(.kts)`, `build.gradle(.kts)`, 버전 카탈로그, Gradle Wrapper를 먼저 확인하고 해당 프로젝트에 정의된 SDK·Kotlin·AGP·JDK 버전을 따른다.
- UI 방식은 기존 코드에 맞춰 Jetpack Compose 또는 XML View를 사용하고, 요청 없이 두 방식 사이를 전환하지 않는다.
- `local.properties`, 키스토어, 서명 정보, API 키 등 로컬 환경 정보와 비밀은 저장소에 추가하지 않는다.

## 검증

- 코드를 변경했다면 대상 프로젝트의 Gradle Wrapper로 변경 범위에 맞는 테스트, lint, build 작업을 실행한다.
- 독립 챕터가 여러 개인 경우 전체 저장소에 단일 Gradle 작업이 있다고 가정하지 않는다.
- 에뮬레이터나 실기기가 필요한 검증을 실행하지 못했다면 그 사유와 대신 실행한 검증을 결과에 남긴다.

## 커밋

- 사용자가 현재 요청에서 `커밋`을 명시적으로 지시한 경우에만 `git commit`을 실행한다.
- 코드 작성, 수정, 리뷰, 검증 또는 작업 완료 요청만으로 커밋 권한을 추론하지 않는다.
- 사용자가 커밋 단위나 메시지를 지정했다면 그대로 따른다.
- 커밋 요청은 명확하지만 단위나 범위가 모호하면 먼저 커밋 구성을 제안하고 사용자 확인을 받은 뒤 실행한다.
- 커밋을 요청받지 않았다면 변경 사항을 stage하거나 commit하지 않고 작업 결과와 검증 내용만 전달한다.
- 여러 챕터의 변경을 커밋할 때는 각 챕터를 별도 커밋으로 분리한다. 공통 문서와 하네스 설정도 챕터 코드와 분리한다.
